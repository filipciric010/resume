# Environment variables for Resume Boost
# Copy this file to .env (local dev) or configure these in your deploy environment

# --- Client (Vite) ---
# Required for Supabase client in the browser
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Toggle AI features in the UI (server still controls actual availability)
VITE_AI_ENABLED=true

# --- Server (Node/Express) ---
# OpenAI API key for AI features (server-side only; never expose to client)
OPENAI_API_KEY=
# Model to use for AI routes
OPENAI_MODEL=gpt-4o-mini
# Origin where the client is hosted (used by the PDF server)
CLIENT_ORIGIN=http://localhost:8080
# Port for the server (PDF/API)
PORT=3001

# --- Optional ---
# If you prefer non-Vite names for scripts/tools
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Frontend
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: leave empty in client; AI calls are proxied via server
VITE_AI_ENABLED=true

# Server
PORT=3001
CLIENT_ORIGIN=http://localhost:8080
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo
